// ============================================================
// PRISMA SCHEMA CONFIGURATION & DATA SOURCE
// ============================================================

generator client {
  provider = "prisma-client"
  output   = "../../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}
